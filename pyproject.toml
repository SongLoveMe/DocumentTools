[project]
name = "documenttools"
version = "2.2.0"
description = "Local Windows document conversion and PDF utilities."
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
